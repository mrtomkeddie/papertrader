# Paper Trader (App Code)

Canonical documentation lives in the project root `README.md`.

Quick start (via root scripts):
- Install: `npm install`
- Dev: `npm run dev`
- Build: `npm run build`
- Preview: `npm run preview`
- Scheduler: `npm run scheduler`

App code, server scripts, and assets live here under `repo/`.